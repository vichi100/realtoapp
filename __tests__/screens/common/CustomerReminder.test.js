import CustomerReminder from '../../../src/screens/common/CustomerReminder';

describe('CustomerReminder screen', () => {
  it('exports a component', () => {
    expect(CustomerReminder).toBeDefined();
  });
});
