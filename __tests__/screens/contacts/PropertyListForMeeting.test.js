import React from 'react';
import Screen from '../../../src/screens/contacts/PropertyListForMeeting';

describe('PropertyListForMeeting', () => {
  it('exports a component', () => {
    expect(Screen).toBeTruthy();
  });
});
